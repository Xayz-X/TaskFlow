export enum StatusCodes {
  SUCCESS = 200,
  OK = 200,
  RESOURCE_CREATED = 201,
  BAD_REQUEST = 400,
  FORBIDDEN = 401,
  NOT_FOUND = 404,
  CONFLICT = 409,
  RATELIMIT = 429,
  INTERNAL_SERVER_ERROR = 500,
}
